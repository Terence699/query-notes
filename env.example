# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Site URL
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# AI Provider API Keys
# At least one of these must be set for the application to work

# SiliconFlow API Key (Primary Provider - Recommended)
# Get your API key from: https://cloud.siliconflow.cn/account/ak
SILICONFLOW_API_KEY=your_siliconflow_api_key

# DeepSeek Official API Key (Fallback Provider)
# Get your API key from: https://platform.deepseek.com/api_keys
DEEPSEEK_API_KEY=your_deepseek_api_key

# Note: 
# - If both keys are provided, SiliconFlow will be used as primary with DeepSeek as fallback
# - If only one key is provided, that provider will be used exclusively
# - SiliconFlow is recommended as it provides better stability for international users 